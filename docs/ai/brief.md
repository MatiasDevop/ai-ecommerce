Create a short "AI README" for your project with:

Stack and versions
Folder structure and main entry points
Conventions (lint, migrations, error handling)
Auth and session model
DB conventions, ORM rules, naming


Always exclude: .env and env files, keys, certs, logs, build artifacts (e.g. dist/, .next/), generated code, and DB dumps. Use a .cursorignore (like .gitignore) and add node_modules/, dist/, build/, .next/, logs/, .env*, *.log, and any generated or dump files.